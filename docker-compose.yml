services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      SECRET_KEY: ${SECRET_KEY:-dev-secret-change-in-production}
      DATABASE_URL: sqlite:////app/data/app.db
    volumes:
      - taskboard_data:/app/data

volumes:
  taskboard_data:
